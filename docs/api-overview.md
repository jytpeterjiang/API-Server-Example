# API Overview

This is a test document created via GitHub MCP Server.

## Endpoints

- `GET /api/users` - List all users
- `POST /api/users` - Create a user
- `GET /api/users/:id` - Get user details

## Authentication

All API requests require authentication via Bearer token.
